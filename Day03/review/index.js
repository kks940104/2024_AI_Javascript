// 프로그래밍 언어 : 컴퓨터에게 명령어를 만드는 언어

// 자바스크립트 언어 : 브라우저(DOM[HTML + CSS] + 기타)를 조작하기 위한 언어

// alert, prompt, console.log : [Input / Output]

// Variables(변수) : const(재할당 불가능), let(재할당 가능), var(절대 사용x)

// Data - Type(타입) :
// * 기본 : String['',"",``], Number[숫자], Boolean[true, false], Null, Undefined
// * 참조 : Array[["",""]] , Object { key[문자 및 정수] : value[any타입], }

// Type-Casting(타입변환)
// * 문자화 : String(변수), + [문자열 연결 연산자]
// * 숫자화 : Number(), parseInt()
// * 불리언화 : boolean(), !변수 -> Truthy & falsy(0, "", null, underfind, [빈배열])

// Operator(연산자)
// * 산술연산자 : +, -, *, /, %, **
// * 논리연산자 : &&(and), ||(or), !(not)
// * 대입연산자 : =
// * 비교연산자 : ==, !=, >, <, <=, <=
// * 삼항연산자 : 조건삭 ? 참 : 거짓

// Condition(조건문) : if(조건식), elae if(if문 다음 조건식), else

// Function(함수)
// function name(매개변수){ 함수. return 사용 }
